# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Petgram' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Petgram
  pod 'Firebase/Core'
  pod 'Fabric', '~> 1.9.0'
  pod 'Crashlytics', '~> 3.12.0'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  pod 'RxSwift'
  pod 'RxCocoa'

  target 'PetgramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PetgramUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
